const randomNumber = require('./rand')

module.exports = function(arrLength) {
    const result = []
    let ind = 0

    while(ind < arrLength) {
        result.push(randomNumber())
        ind++
    }

    return result
}